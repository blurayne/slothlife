apiVersion: v1
kind: Service
metadata:
  name: {{ include "slothlife.fullname" . }}
  labels:
    {{- include "slothlife.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "slothlife.selectorLabels" . | nindent 4 }}
